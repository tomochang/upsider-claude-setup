## Session Startup
- Always start by changing to ~/clawd directory

## Basics
- 日本語で応答する
- 困ったことがあれば何でも聞いてください。コード、調査、レビュー、ドキュメント作成、何でもできます
- エラーが出たら自分で調査して解決を試みる。同じコマンドを繰り返さない
- ファイルを編集したら `cd ~/clawd && git add -A && git commit -m "<変更内容>" && git push` で保存する
